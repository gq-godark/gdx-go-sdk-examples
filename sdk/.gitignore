/bin/
/dist/
/build/
/coverage/
*.test
*.out
*.prof
coverage.txt
.env
.DS_Store

# vendor/ is intentionally NOT ignored; if a future commit vendors deps we
# want them under source control. Until then it just won't exist.

# compiled example binary
/quickstart

# compiled example binaries
/m8_rate_limit
