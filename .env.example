# GoDark API credentials (testnet). Obtain from the GoDark dashboard.
GODARK_API_KEY_ID=
GODARK_API_SECRET=

# Optional: override the edge WebSocket origin. Defaults to the production
# testnet endpoint baked into the SDK if left unset.
# GODARK_EDGE_URL=wss://api.godark-dex.com
